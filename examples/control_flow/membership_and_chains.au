def in_range(value: int32, low: int32, high: int32) -> bool:
    return low <= value < high

def main():
    mut ports = list[int32]()
    ports.append(80)
    ports.append(443)

    mut allowed = set[str]()
    allowed.add("aura.dev")

    mut ages = dict[str, int32]()
    ages["ada"] = 36

    request = "GET /health HTTP/1.1"

    print(443 in ports)
    print(8080 not in ports)
    print("aura.dev" in allowed)
    print("ada" in ages)
    print("/health" in request)
    print(in_range(80, 1, 1024))
    print(in_range(2048, 1, 1024))
