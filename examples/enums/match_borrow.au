def main() -> int32:
    result: Result[str, str] = Result.Ok("ok")

    match result:
        case Ok(value):
            print(value.clone())
        case Err(message):
            print(message)

    return 0
