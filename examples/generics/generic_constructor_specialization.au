class Box[T]:
    value: T

def main() -> int32:
    box = Box[int32](value=42)
    jobs = Queue[int32](capacity=4)
    jobs.put(box.value)
    jobs.close()

    for value in jobs:
        print(value)

    return 0
