class Box[T]:
    value: T

def main() -> int32:
    box = Box[int32](value=42)
    jobs = Channel[int32](capacity=4)
    jobs.send(box.value)
    jobs.close()

    for value in jobs:
        print(value)

    return 0
