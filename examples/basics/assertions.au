def validate(value: bool, message: str):
    assert value, message

def main():
    print("checking")
    actual = 2 + 2
    expected = 4
    assert actual == expected, "arithmetic changed"
    values = [1, 2, 3]
    assert 2 in values
    validate(true, "validation changed")
    print("all assertions passed")
