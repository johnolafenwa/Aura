trait Displayable:
    def display(borrow self) -> String

class Box[T]:
    value: T

impl Displayable for Box[String]:
    def display(borrow self) -> String:
        return self.value.clone()

def main() -> int32:
    box: Box[String] = Box(value="hello")
    print(box.display())
    return 0
