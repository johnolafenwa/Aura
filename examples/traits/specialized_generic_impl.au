trait Displayable:
    def display(self) -> str

class Box[T]:
    value: T

impl Displayable for Box[str]:
    def display(self) -> str:
        return self.value.clone()

def main() -> int32:
    box: Box[str] = Box(value="hello")
    print(box.display())
    return 0
