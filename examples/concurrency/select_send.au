def main() -> int32:
    jobs: Queue[int32] = queue()

    select:
        case send_result = jobs.put(4):
            match send_result:
                case Result.Ok(done):
                    print("sent")
                case Result.Err(err):
                    print("failed")
        case after(5ms):
            print("timeout")

    match jobs.get():
        case Option.Some(value):
            print(value)
        case Option.None:
            print(0)

    return 0
