def producer(out: Queue[int32]):
    out.put(9)
    out.close()

def main() -> int32:
    jobs = Queue[int32]()
    with TaskGroup() as group:
        group.start_soon(producer, jobs)
        print(jobs.get_or(0))

    return 0
