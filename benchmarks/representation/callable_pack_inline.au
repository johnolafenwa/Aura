type Adder = Callable[def(value: int64) -> int64]

def make(offset: own int64) -> Adder:
    return Adder(lambda value: value + offset)

def main():
    mut total = 0
    mut index = 0
    while index < 100000:
        add = make(index)
        total = total + add(1)
        index = index + 1
    print(total)
