class Label:
    text: str

def combine(a: Label, b: Label) -> str:
    return a.text + " " + b.text

def main():
    x = Label(text="hello")
    y = Label(text="world")
    result = combine(x, y)
    print(result)
    print(x.text)
