trait Displayable:
    def display(self) -> str

class Box[T]:
    value: T

impl Displayable for Box[int32]:
    def display(self) -> str:
        return "box"

def main():
    b = Box(value=42)
    print(b.display())
