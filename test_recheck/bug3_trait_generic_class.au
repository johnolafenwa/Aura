trait Displayable:
    def display(self) -> String

class Box[T]:
    value: T

impl Displayable for Box[int32]:
    def display(self) -> String:
        return "box"

def main():
    b = Box(value=42)
    print(b.display())
