# 2026-03-15 Examples README Session

## Goal

Add a focused README under `examples/` so the current sample programs are easy to run from the repo without guessing the CLI commands.

## Changes

- added `examples/README.md`
- documented the current example set
- documented `run`, `check`, and `ast` commands using the `aura` CLI
- included the expected output for `examples/point.au`

## Verification

- instructions were matched against the current CLI implementation in `crates/aura/src/main.rs`
