# Phase 5.9: configurable blocking-I/O pool

- Date: 2026-07-27
- Status: implementation and repository gates complete; clean-tree benchmark
  pending

## Goal

Implement Provisional ADR-0035 so operators can select the process-wide
blocking-I/O worker count and optionally bound the accepted pending queue,
without blocking Aurora scheduler workers or weakening timeout and
cancellation behavior.

## Documentation completed

- Replaced the stale fixed, nonconfigurable, unbounded-only pool description
  across the architecture guide, Manual, Learn track, tutorials, README, and
  changelog.
- Documented `AURORA_BLOCKING_WORKERS` as an exact positive, unclamped
  override, with the absent default derived from host parallelism, fallback
  `4`, and clamp `2..=8`.
- Documented `AURORA_BLOCKING_QUEUE_CAPACITY` as an optional positive bound on
  accepted pending jobs only. Running jobs and callers awaiting admission do
  not consume the bound; omission preserves the unbounded compatibility mode.
- Documented FIFO scheduler-aware admission, pre-acceptance timeout and
  cancellation, the non-retractable accepted-job boundary, late-result
  disposal, lazy initialization, and fatal pre-user-code `AU4006` validation
  across MIR, direct, and standalone execution.
- Kept the limitation explicit: bounding pending jobs cannot interrupt an
  accepted host call or guarantee unrelated progress while every blocking
  worker remains stuck.
- Added reference-integrity pins and a stale-contract rejection guard.

## Verification

- Focused blocking-I/O configuration, lifecycle, admission, saturation,
  timeout/cancellation-race, panic-containment, resolver/TCP, and scheduler
  tests are green. The final blocking-I/O slice contains 26 tests.
- Product-path regressions cover invalid configuration before user code,
  bounded admission and acceptance-boundary parity, and MIR/direct/standalone
  execution. A one-worker queue-stress regression also removed an invalid
  fixture assumption that every consumer must receive work; Aurora guarantees
  integrity and progress, not per-consumer scheduling fairness.
- Exact full `npm run ci` is green on the settled implementation:
  45 benchmark-harness tests, 296 CLI tests, 1,130 compiler library tests,
  every fixture and package/module/network/process suite, the serialized
  forced-backend parity matrix in 837.22 seconds, 89 LSP tests, 13 extension
  tests, both coverage gates, reference integrity, docs, both audits,
  warning-denied Clippy, and hygiene.
- Frozen compiler coverage is green at 70,514/73,341 lines
  (96.14540298059748%), 4,683/4,830 functions (96.95652173913044%), and
  103,704/109,782 regions (94.4635732633765%), above the unchanged
  96.13/96.89/94.35 floors.
- No synthetic coverage test or exclusion was added. Two impossible
  post-registration/post-removal admission-fill loops and one contradictory
  nested wake arm were restructured away; observable tests pin impossible
  worker-vector reservation failure, shutdown rejection, cached startup
  failure, and zero rejected-job execution.
- `git diff --check`, `bash -n scripts/check-reference.sh`,
  `npm run check:reference`, and `npm run docs:build` all pass.
- Build-artifact hygiene was maintained: an earlier broad cleanup reclaimed
  36.4 GiB; after full CI `target/` is 15 GiB with 163 GiB available.

## Remaining

- Commit the settled implementation and verification record.
- Run the contractual clean-tree Phase 5.9 benchmark from that implementation
  commit and record its raw-report hash, hardware context, and gate results.
- Commit the benchmark evidence, mark Phase 5.9 complete, then begin
  Phase 5.10 in the authorized strict order.
