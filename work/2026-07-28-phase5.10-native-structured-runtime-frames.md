# Phase 5.10: native structured runtime frames

- Date: 2026-07-28
- Status: complete and checkpoint-ready under the Part 3 benchmark escape hatch

## Goal

Implement provisional ADR-0036 so MIR, direct-native, standalone, CLI JSON,
and editor-tooling surfaces share one typed Aurora call-frame and task-ancestry
model. Remove the deferred three-note parity carve-out without changing
ordinary nonzero program statuses, cleanup precedence, or human diagnostic
spelling.

## Bound contract

- Every structured diagnostic carries `call_frames` and `task_ancestry`
  arrays, including empty arrays for compile-time and pre-user-code failures.
- Call frames are innermost first; task ancestry is youngest first. Every
  frame carries its own defining or spawning source path.
- Runtime frames are captured exactly once before cleanup or task-state reset.
  Human frame prose is synthesized by the renderer and is absent from
  structured `notes`.
- Public diagnostic schema version `1` gains the arrays additively; readers
  ignore unknown members. The compiler-service semantic interface remains
  version `2`.
- JSON-mode direct runs use a private trap-signal pipe plus a separate bounded
  JSON data pipe so a program trap, missing record, and ordinary
  `main() -> int32` nonzero status remain distinct. Native initialization
  hides and marks both descriptors close-on-exec before user code. Human CLI
  and standalone behavior remain direct-rendered.
- Only direct build/launch unavailability may trigger `auto` fallback.
  Post-launch trap, wait, signal, and diagnostic-protocol outcomes are final.
- Both parity normalizers are removed, and every maintained run-fail oracle
  pins the complete human diagnostic.

## Implemented

- Added typed diagnostic call frames and task-ancestry records, additive
  schema-v1 JSON fields, version-2 compiler-analysis records, and exact LSP
  forwarding. Older compatible analysis records default missing arrays to
  empty.
- Added per-module defining and spawning paths, innermost-first call stacks,
  youngest-first task ancestry, and once-only capture before cleanup or task
  state retirement on both execution backends.
- Added native task-local frame storage, depth rejection, reset and key-reuse
  isolation, and exact codegen metadata forwarding through the private runtime
  ABI.
- Replaced the first implementation's eagerly owned native frame vectors and
  strings with validated immutable metadata, inline depth-one call storage,
  persistent shared task ancestry, and trap-time materialization. The scope
  guard no longer retains the full previous task state inline, ancestry
  teardown is iterative, and repeated propagation of an already captured
  diagnostic performs no frame rematerialization.
- At commit `c3278c4`, boxed each native task state and prebuilt the box on the
  spawning task before the child coroutine enters its runtime scope. A narrow
  `PreparedDirectTaskRuntimeState` handoff is the only new `Send` safety
  boundary: it can contain only pristine state, is consumed exactly once
  before Aurora user code runs, and does not make a live task state `Send`.
  The installed box retains its allocation identity while the task-state map
  stores only a pointer-sized value. Moving state construction off the child
  coroutine stack and isolating installation in a non-inlined helper reduces
  the task-scope setup frame from 1,312 bytes to 432 bytes.
- Added the two-pipe native JSON protocol: an exact trap-intent marker plus a
  separate bounded EOF-delimited structured record. Native initialization
  takes ownership of both descriptors, marks them close-on-exec, and removes
  both private environment variables before Aurora user code can run.
- Made direct transport validation exact. Ordinary nonzero exits remain
  ordinary statuses; signal termination and missing, malformed, oversized,
  contradictory, or repeated protocol records are hard execution failures.
  Only pre-launch build or launch unavailability may trigger automatic MIR
  fallback.
- Removed both diagnostic-note parity normalizers. All 53 maintained run-fail
  oracles now pin the complete rendered Aurora call chain.
- Updated the maintained manual, tutorials, READMEs, architecture documents,
  changelog, ADR index, and executable reference integrity pins.

## Verification

- The complete compiler library passes: 1,150 tests at settled commit
  `181204b`.
- Focused MIR backtrace coverage passes, including cleanup-primary failure
  capture and exact once-only frame rendering.
- Focused CLI direct-JSON coverage passes across cold, warm, waiting, and
  automatic paths. Grandchild descriptor containment, virtual stdin paths,
  cross-module task starts, and exact MIR/direct structured equality pass.
- The compiler analysis bridge passes its focused compatibility tests. The
  language server passes all 90 tests at 100% statement, branch, function, and
  line coverage.
- All 53 regenerated run-fail oracles match current MIR behavior without a
  parity mask.
- Reference integrity passes, including re-execution of maintained Manual
  blocks and rejection of stale frame-contract prose. The documentation build,
  compiler and CLI checks, formatting, and scoped diff checks pass.
- Independent integration review found three transport defects: post-launch
  automatic fallback, descendant descriptor/environment leakage, and child
  human output after a signalled record failure. All three are fixed and
  regression-tested.
- The complete serialized forced-backend parity matrix passes in 669.49
  seconds across 239 run-pass and 53 run-fail fixtures.
- No synthetic coverage test or exclusion was added. Exact clean-commit
  coverage at settled commit `181204b` is 71,153/74,016 lines
  (96.131917%), 4,757/4,909 functions (96.903646%), and 104,478/110,598
  regions (94.466446%). The one-time final floors are therefore
  96.13/96.90/94.46. LSP statement, branch, function, and line coverage
  remains 100%.
- Exact clean CI at settled commit `181204b` passes every product gate,
  including formatting, benchmark-harness tests, the Rust workspace,
  serialized backend parity, 90 LSP tests, 13 extension tests, compiler and
  LSP coverage, reference integrity, docs, audits, warning-denied Clippy, and
  hygiene. Preserve
  `/private/tmp/aurora-phase510-state-prebuilt-coverage-closure-ci.log`,
  SHA-256
  `0776403c16bd356cb46d42b1e3dcc19c0c09a0ebf29be0e0cf2e405f6fa6c910`.
- The compact correction's focused frame, ancestry, depth, suspension,
  forced-reset, cleanup, cancellation, formatting, and warning-denied library
  Clippy checks pass. The native call-entry stack frame is 208 bytes after the
  correction, down from 528 bytes in the rejected implementation.
- The third correction's focused frame propagation, ancestry, 1,000 suspended
  task isolation, task-key reuse, forced exit, cancellation, formatting,
  release build, and warning-denied library Clippy checks pass. Its
  pointer-size and allocation-identity regression test confirms that the map
  stores an 8-byte box instead of the former 280-byte inline state while
  preserving the same allocation through installation.
- The final contractual report's three 10,000-sleeper peaks are 207,798,272,
  206,946,304, and 206,831,616 bytes, all comfortably below the 512 MiB gate.

## Rejected benchmark evidence

- The first contractual report, from implementation commit
  `29ff7f606e3c0320c590947291a8f041db9e15cb`, is valid and clean but rejected:
  100,000 sleepers plus 1,000 timers peaked at 2,040,184,832 bytes against the
  1.5 GiB gate, up 582,336,512 bytes from the accepted Phase 5.9 report. The
  stable 10,000-sleeper delta was about 1.43 KiB per task. Audit traced the
  regression to eagerly retained owned call/task frame vectors and strings
  plus coroutine-stack page pressure. Preserve the raw report at
  `/private/tmp/aurora-phase510-after-native-frames.json`, SHA-256
  `012feee3e8a840c1c59c3c503572188b7bc4a8cc72b1ac3f30b5bb53f49f4528`.
- The first report's third standalone-timer run was also rejected as host
  noise at a 14 ms arm span and 7 ms p99; its massive-workload timers passed
  all three runs at 3-4 ms arm spans and 2 ms p99.
- Exact clean CI is green at coverage-closure commit `e171420`, but the
  compact-frame contractual report from that commit is rejected only on
  massive RSS. Its three peaks are 1,095,598,080, 1,629,388,800, and
  1,831,649,280 bytes against the 1,610,612,736-byte limit; every timer,
  10,000-task, idle, starvation, and multicore gate passes. Release
  disassembly traced the remaining nonlinear resident-page amplification to
  the former 1,312-byte task-scope setup frame. Preserve
  `/private/tmp/aurora-phase510-after-native-frames-compact.json`, SHA-256
  `64868869cec520b438594214d8b62e0691cf921b8d062a1337fd0be82280ca60`.

## Final benchmark disposition

- The complete contractual benchmark was run from settled commit `181204b`.
  Preserve
  `/private/tmp/aurora-phase510-after-native-frames-state-prebuilt.json`,
  SHA-256
  `8ba448a06a8efb505af723ed00b8248fc1aa44ed270b46df5c15d74ecb9bd986`,
  produced by release binary SHA-256
  `50503389792f7f86efb8f021f983a3917855bad82e4fbc90b99414695331142a`.
  Preserve the runner log at
  `/private/tmp/aurora-phase510-after-native-frames-state-prebuilt.log`,
  SHA-256
  `4c8d5cf7149b0b224847ae8c9e3ba49b5b26e80d8e5e2d20559457e91e3a683b`.
- The 10,000-sleeper, standalone and massive-workload timer, idle CPU,
  starvation-latency, and multicore-scaling gates all pass. The only failing
  gate is massive-concurrency RSS: the three 100,000-sleeper plus 1,000-timer
  peaks are 1,170,735,104, 1,921,531,904, and 2,001,305,600 bytes against the
  1,610,612,736-byte limit.
- On the recorded 16 KiB-page host, one resident page for each of the 101,000
  stackful tasks is already a 1,654,784,000-byte floor, above the 1.5 GiB
  limit before scheduler, heap, code, and process overhead. After three
  production memory corrections and three clean contractual reports, the
  threshold is therefore unreachable for this stackful-coroutine architecture.
- Invoke the Batch 4 Part 3 escape hatch explicitly: retain the measured best,
  withdraw the 100,000-sleeper "massive concurrency" marketing claim from
  README and documentation surfaces, and continue to the checkpoint because
  the mandatory multicore gate passes. This disposition does not weaken or
  waive any other benchmark gate.

## Checkpoint disposition

Phase 5.10 is complete at settled commit `181204b`. The implementation,
behavioral coverage, exact clean CI, and all non-massive-RSS contractual gates
are green. The sole red gate is closed through the prompt's explicit,
evidence-backed Part 3 escape hatch and associated claim withdrawal. No
synthetic coverage test or justified exclusion exists. The phase is ready for
the Batch 4 checkpoint; Phase 6 has not begun.
