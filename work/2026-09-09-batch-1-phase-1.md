# Batch 1 phase 1: type foundations and owned callables

## Target and authorized scope

Implement the ratified design at `7685199` on `codex/batch-1-phase-1`, through
the phase 1 checkpoint, green branch CI, a merge commit, and green main CI.
This work note uses the filename requested by the user. Ratification date:
2026-09-08. The design wins over conflicting ADR baseline text.

Phase 2 is excluded: retain Option library signatures, `T?`, and `*_or_none`
names. Preserve the ten Approved Decisions and the protected personal file,
`.swp`, and untracked ADR-0022 draft. No version bump, tag, release preparation,
scheduler change, backend switch, runtime crate split, or frame-contract change.

## Current status

Stage 1 records are committed as `b05ddda`. The pre-extraction baseline and E2.1 (`83ee945`) are green. E2.2
places/loans is complete (`38c995c`). E2.3 capabilities/callables/resolution
is complete; all required extraction gates pass before any feature.
The parser and normalization/alias families have passed their required gates.
Union/alias coverage includes canonical identity, local/imported declarations,
bounds, visibility, constructor/member adaptation, and bounded expansion.
Injection is next; later callable, generic, layout, interface, and editor
integration remains tracked by its owning family. The required extraction
preceded every feature, with one commit and unchanged fixtures per step. Initial build hygiene: target 13 GiB, available disk 107 GiB.

## Execution and evidence ledger

| Stage or family | State | Required evidence |
| --- | --- | --- |
| Stage 1 records | Complete | Ratified answer links, dated dependent amendments, one documentation commit |
| E2.1 program/types/properties | Complete | Characterization and unchanged complete fixtures, CLI, native acceptance |
| E2.2 places/loans | Complete | Same gates; place/loan/return-origin behavior unchanged |
| E2.3 capabilities/callables/resolve | Complete | Same gates plus forced parity and unchanged coverage floors |
| Parser surface | Complete | Failing H1 fixtures first; contextual syntax and callable grammar |
| Union normalization and aliases | Complete | Canonical/singleton/generic alias identity and cycle diagnostics |
| Injection | Pending | Unique member, ambiguity, evaluation and capability oracles |
| Type patterns and exhaustiveness | Pending | Modes, coverage, guards and tag locks |
| Narrowing and flow | Pending | Stable places, short-circuit flow, invalidation and exit joins |
| Generic members | Pending | Substitution, canonical remapping and universal checking |
| Union properties | Pending | Copy/clone, Q6 B equality/hash, Transfer, traits and rendering |
| Layout/interfaces/drop plans | Pending | Shared MIR/direct plans, ABI, cleanup and schema rejection |
| Nullable handle FFI results | Pending | Linked C shim, nullable/non-nullable behavior and ABI order |
| Callable contracts | Pending | Names/defaults/keyword-only, invariance and explicit restrictions |
| Callable packing and call kinds | Pending | Inline/overflow allocation, tables, cleanup and AU4005 |
| TaskCallable targets | Pending | Structural admission and all four starts on both backends |
| Bound methods | Pending | Owned/Copy capture, call kinds, cleanup and snapshot behavior |
| Stored argument-origin views | Pending | Origin contracts, containment and rejected captured-self origins |
| Existing Shared callback sites | Pending | Packed values accepted without weakening call contracts |
| LSP and extension | Pending | Compiler-owned semantics, 100% coverage, package verification |
| Maintained documentation | Pending | Manual/tutorial/example/LLM/changelog consistency and gates |
| Reference agent phase 1 | Pending | Under 400 lines, pinned MIR/direct stdout; separate size/allocation evidence |
| Full local verification and ratchet | Pending | All requested gates; floors raised only to final measured values |
| Hosted integration | Pending | Final-head branch green, merge commit, main green |

## Extraction baseline

- All nine complete compiler fixture suites pass before extraction.
- Two public semantic characterization tests pass: byte-exact Type JSON with
  nested types and calling metadata, and exported generic ownership/default
  declarations through the existing facade.
- The [fixture baseline](evidence/batch-1-phase-1/extraction-baseline.json)
  records 1,807 tracked files (968 Aura sources) across nine categories.
- All 378 CLI tests and the native-codegen acceptance test passed before extraction.
- The first move extracts program/type definitions and pure structural
  property helpers, retaining facade entrypoints and serialized meanings.

### E2.1 extraction

- `sema/program.rs` owns checked declarations, module inventory, and program
  checking; `sema/types.rs` owns type identity, lowering, and substitution;
  `sema/properties.rs` owns structural property derivation.
- The facade keeps exported types and the existing `check_with_context`
  entrypoint. Its body delegates to the unchanged program checker.
- All 434 existing function bodies and signatures compare identically as Rust
  tokens after ignoring extraction visibility, comments, whitespace, and
  formatter-added trailing commas. All nine fixture-category hashes remain
  identical to the baseline.
- Production `cargo check -p aura-compiler` passes without warnings.
  Post-move verification passes: 1,885 compiler unit tests, nine fixture
  suites, two semantic characterizations, 378 CLI tests, and one native-codegen
  acceptance test. No test expectation changed. Build hygiene after the gate:
  target 15 GiB, available disk 104 GiB.
- Before E2.2, the existing semantic regressions pin disjoint projections,
  dynamic same-root returned-view alternatives, transitive footprints, deep
  reborrow suspension, and arm-order-independent branch expiry. Existing CLI
  cancellation/acceptance tests remain part of each extraction gate.

### E2.2 extraction

- `sema/places.rs` owns canonical paths, fixed projections, overlap, and
  expression-to-place helpers. `sema/loans.rs` owns provenance, returned-view
  footprints, last use, reborrows, and loan/access conflicts.
- All 434 original function signatures/bodies retain the same token comparison
  as E2.1, and all nine fixture category hashes remain unchanged.
- Post-move verification passes: all 404 semantic unit tests, nine complete
  fixture suites, two interface characterizations, 378 CLI tests, and one
  native-codegen acceptance test. No feature or expectation change is included.
- Build hygiene after the gate: target 15 GiB, available disk 103 GiB.

### E2.3 extraction

- `sema/capabilities.rs` owns declaration-stable passing modes, ownership
  acquisition, and mutable-access requirements. `sema/callables.rs` owns
  callable contracts, lambda checking, target resolution, and default/argument
  binding. `sema/resolve.rs` and `sema/traits.rs` own visible declaration lookup,
  canonical nominal names, trait bounds, and method applicability.
- Every extracted module now lists explicit dependencies. The facade preserves
  existing public types and entrypoints. Remaining expression/body coordination
  is split with E2 steps 4–6 and their enabling feature families.
- All 434 original functions retain the extraction token comparison and all
  nine fixture category hashes are unchanged. Post-move verification passes:
  1,885 compiler unit tests, nine complete fixture suites, two interface
  characterizations, 378 CLI tests, and one native-codegen acceptance test.
- Forced parity passes for all 291 top-level run-pass and 94 run-fail fixtures:
  both explicit backends match the pinned stdout/diagnostic oracles, without
  fallback. Nested support sources are counted separately in the baseline.
- The full workspace coverage gate passes with four test workers. Its
  archive exclusion, filename ignore expression, and 96.30/97.21/94.71 floors
  are identical to `scripts/coverage-compiler.sh`; that tracked script is
  unchanged. No timing failure or isolated rerun occurred. Measured coverage:
  **96.3147504888% lines / 97.2210285059% functions / 94.8212872384% regions**.
  Floors remain unchanged; the upward ratchet waits for final feature code.
  [Step-3 evidence](evidence/batch-1-phase-1/extraction-3.json) records totals
  and coverage for the facade and all nine extracted modules.
- After the test processes exited, `cargo clean -p aura -p aura-compiler`
  removed obsolete workspace package artifacts while retaining dependency
  caches. Target decreased from 16 GiB to 5.4 GiB; available disk increased
  from 102 GiB to 113 GiB before coverage. After coverage: target 11 GiB,
  available disk 108 GiB. No feature has landed.

## Stage 1 records

- ADR-0052/0058 record the detailed ratification and question-to-section links.
- ADR-0033/0037/0038/0044/0051 receive dated extensions for the new contracts.
- FFI v0 has no dedicated ADR; its Phase 6.4 record in
  [the Batch 5 work note](2026-07-29-batch5-callables-closures-ffi.md)
  receives the dated Q10 nullable-handle result amendment.
- The roadmap and indexes distinguish the active phase 1 work from phase 2.

## Design conflicts and backend reproductions

No implementation conflict or Cranelift reproduction identified yet.
The known ADR discrepancies are reconciled under the ratified design; they
are not implementation blockers. Security-sensitive regression work will use
the required Daybreak Blue delegation when that bounded work is reached.

### Daybreak Blue regression preparation

The read-only `phase1_security_regressions` sub-agent identified deterministic
seams for the required security-sensitive tests; it changed no files and ran
no builds or resource loads. These are planned tests, not completed coverage:

- Semantic interface schema 6 and native-cache format v6 currently participate
  in the CLI cache key. Pin each new schema/layout identity independently.
- Serialized MIR currently has no version envelope; extend its shared validator
  before accepting new union/callable metadata, with tiny mutate-after-lowering
  tests for invalid canonical members, tag maps, layouts and ownership plans.
- Test generic substitution/normalization using small nested and duplicate
  member sets, with one canonical producer and validator.
- Extend structural Transfer admission with precise forbidden capture paths,
  and reject asserted TaskCallable metadata lacking concrete evidence.
- Use deterministic allocation budgets for overflow/over-alignment, prefix
  initialization failure and reverse cleanup; never rely on host exhaustion.
- Preserve the cache's existing digest, symlink, executable-shape and
  concurrent-replacement protections. Reuse those small test seams.

## Verification

Stage 1: 14 scoped documents and 172 relative links/anchors passed, question
coverage and statuses passed, Approved Decisions remain byte-identical,
scoped whitespace passed, and all 15 identity tests passed. No implementation
feature verification is claimed yet. E2.1 structural verification is recorded
above. Coverage floors remain
96.30% lines / 97.21% functions / 94.71% regions; LSP remains 100%.

## Parser family

- Added ten parse-pass and fourteen diagnostic-pinned parse-fail fixtures before
  grammar implementation. Five initial AST tests failed; all now pass, together
  with two additional AST tests. A newly failing annotation/specialization test
  exposed and fixed union/keyword-boundary lookahead.
- Union/grouped type syntax, contextual generic/public aliases, callable slot
  names/default promises, keyword-only declarations/lambdas, owned callable
  constructors, and contextual None tests preserve their AST metadata.
- Migrated three formerly rejected forms explicitly accepted by ratification:
  keyword-only parameters, None tests, and parenthesized type grouping. No
  unrelated diagnostic expectation changed. Retired the Python `is None`
  migration hint into a parse-pass fixture because `is` is now accepted.
- Expression/type walkers preserve operands and spans. The grammar family
  deliberately rejects the newly parsed union/owned-callable types, aliases,
  and None tests at semantic checking until their ordered semantic families
  land. Infallible analysis/lowering helpers use their existing unresolved-type
  sentinel for these not-yet-checked forms. These staging guards must be removed
  by the corresponding families before phase 1 completion.
- Verification: 84 existing parser unit tests, seven new AST tests, all nine
  fixture suites, all 385 forced MIR/direct cases, 378 CLI tests, and native
  acceptance pass. The full compiler run passed 1,883 tests and exposed two
  old JSON assertions; the corrected assertions pass with all eleven AST
  tests. All 96 compiler-bridge tests, two semantic-schema tests, formatting,
  and Clippy pass. [Parser evidence](evidence/batch-1-phase-1/parser.json)
  records counts and verification-log hashes. New callable AST metadata changes exported declaration JSON;
  schema 7 is synchronized across compiler, LSP, protocol tests, and maintained
  protocol documentation. Rust and LSP version tests failed first. Existing
  native cache keys already bind the semantic schema, so old checked metadata
  cannot hit a schema-7 cache key. MIR/native-format identities remain tracked
  with their semantic-format families; no public package version changes.
- CLI formatting is whitespace-only and preserves this syntax without changes.
- Post-gate hygiene: target 12 GiB, available disk 107 GiB; no cleanup required.
- Next family red evidence: eight ordinary type/registry tests (seven semantic
  tests plus the AU2010–2015 reservation assertion), five bounded Daybreak alias
  tests, ten check-pass fixtures, and two diagnostic-pinned check-fail fixtures.
  No union or alias semantic implementation preceded these failures.

## Handoff to phase 2

INCOMPLETE — phase 1 is in progress. The final handoff will record the merge,
per-family fixtures and backend results, diagnostic first fixtures, reference
agent commit/output/lines/bytes/allocations, touched Manual/tutorial pages,
conflicts and reproductions, measured coverage and floors, and all schema and
cache identity changes. Phase 2 must not begin from this in-progress record.

### Union normalization and aliases

Failing tests were installed before each semantic seam. The latest focused
suite passes 26 public type/alias tests. Security coverage includes six alias
regressions, seventeen expansion/key-budget helper tests, two structural-key
growth tests, and ten strict union-metadata tests. All nine complete fixture
suites pass. Fifteen new runtime fixtures cover scalar/singleton annotations,
nominal/generic/fixed/nested-inferred constructors, associated and enum members,
and named/qualified/renamed-module imports, including private aliases and renamed
module annotations inside imported bodies.
The complete forced MIR/direct family gate passes.

Alias declarations and checked expansions retain names and defining modules.
Named/renamed/qualified imports, unused target and bound checks, lexical generic
scopes, specialization bounds (including constructors), and public visibility
checks are implemented. Nested and repeated constructor parameters use the
existing nominal inference followed by alias-pattern validation. Diagnostics
for written alias arguments and binding annotations retain the alias plus its
expansion. Full source-facing tooling and checked interface presentation remain
part of their designated phase 1 integration work.

Union identities flatten/deduplicate, collapse singletons, and preserve
module-qualified structural keys. Available callable metadata includes names,
default promises, and keyword-only boundaries. Invalid/incomplete member
fixtures pin AU2010; function-return union display preserves parentheses.
Analysis and builtin annotation helpers now retain normalized union members.

Security review fixed recursive re-substitution of replacement parameters and
exponential escaping of nested canonical keys. Iterative node/depth/aggregate
accounting and key-byte preflight precede expansion. Strict UnionType decoding
rejects noncanonical or forged member metadata before key recomputation.
Constructor aliases share the semantic compilation budget; already-checked MIR
lowering retains per-result guards without charging the semantic aggregate a
second time. Tiny regressions pin both the aggregate limit and unchanged
accounting during repeated lowering. MIR's legacy infallible adapter still
assumes the shared checked expansion cannot fail a per-result guard.

The first full compiler-unit run during this family passed 1,884 and failed nine
imported-trait cases. Qualified checked bounds and origin-aware lookup repair
those cases; all 41 focused imported regressions pass. Existing fixture error
wording is retained when unambiguous, while same-spelling trait collisions keep
qualified identities in diagnostics. The latest complete fixture gate passes
without modifying existing diagnostic expectations. The full unit gate passed 1,903 tests and exposed one old closure JSON
assertion; its explicit keyword_only update passes separately. After the
renamed-module fix, all 41 import regressions, 17 budget tests, two annotation
helper tests, all 26 public type/alias tests, metadata/security tests, and all
nine fixture suites pass again. Clippy is green. The first CLI attempt was
invalidated by concurrent source edits because CLI tests rebuild the runtime;
that owned run was stopped and its four unopened temporary directories removed.
The complete frozen-source gate passes: 378 CLI tests, native acceptance, and
306 run-pass plus 94 run-fail fixtures on both forced backends, fallback disabled.
Thirteen rendered diagnostic fixtures promote the tested cycle/bound/visibility/
inference oracles; all nine fixture suites pass after they are installed. The
family adds 49 top-level fixtures: 10 check-pass, six check-path-pass, eighteen
check-fail, and fifteen run-pass. Full LSP coverage passes all 111 tests at
100% statements (944/944), branches (254/254), functions (49/49), and lines
(944/944). Formatting and Clippy pass.
[Family evidence](evidence/batch-1-phase-1/normalization-aliases.json) records counts,
log hashes, and build hygiene. No existing fixture oracle was changed.

Semantic interface/schema 7 advances to 8 for normalized types and callable
keyword metadata. Stale schema 7 is rejected by the LSP; current/missing/stale
schema bridge tests and the two compiler schema tests pass. Native cache keys
already bind this schema independently; MIR layout/cache-format identities
will be finalized with their owning family. Type JSON characterization pins
the new keyword_only field explicitly.

Complete callable contracts, context-free generic substitution/tag remapping,
and canonical MIR trait dispatch belong to their planned feature families;
none is claimed complete here. Build hygiene before the full unit gate: target
17 GiB, available disk 102 GiB. After parity: target 19.65 GiB, available disk
99.68 GiB. `cargo llvm-cov clean --workspace` completed after stale coverage
outputs were no longer needed. With no open handles, the remaining obsolete
coverage dependency tree was removed with `cargo clean --target-dir
target/llvm-cov-target`. After cleanup: target 19.22 GiB, free
100.09 GiB. Coverage floors and package versions are unchanged.
