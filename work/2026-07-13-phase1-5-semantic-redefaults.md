# Phase 1.5 Semantic Re-defaults

## Session

- Started: 2026-07-13 13:58:22 BST.
- Paused: 2026-07-13 15:16:57 BST after 1h 18m 35s.
- Resumed: 2026-07-13 16:40:01 BST after S1-S4 ratification.
- Paused for D3 gate escalation: 2026-07-13 18:10:47 BST after a further 1h 30m 46s (2h 49m 21s total active elapsed).
- Resumed: 2026-07-13 19:50:02 BST after G1-G3 ratification.
- Stop rule: complete Phase 1.5 and report, stop immediately on a ratified-rule gate failure or unforeseen ambiguity, or reach 12 continuous hours.

## Goal

Preserve accepted Phase 1 in logical commits, complete verification cleanup tickets V1-V5, unbox direct-backend int64/uint64 with measured parity, and execute the breaking Phase 1.5 migration in the ratified D3, D2, D4, D5, D6 order with a full gate and separate commit for every decision.

## Work completed

- Recorded the substantial-work session before beginning commit surgery or implementation.
- Preserved accepted Phase 1 as four logical commits: forced backend harness, semantic recovery, runtime hardening, and documentation/ADRs.
- Completed V1 by restoring an exact check-fail pin for mutable binding of a shared borrowed value without tripping D9 borrowed-return containment first.
- Completed V2 with a debug teardown assertion and failing-first regression that forbids suspended direct-generated tasks from reaching scheduler destruction with forced-exit cleanup still attached.
- Completed V3 with symmetric focused diagnostics for comparisons between `None` and non-optional values.
- Completed V4 by making Phase 4's real `aura run --backend mir` selector and same-change parity-harness update a durable architecture requirement.
- Completed V5 by documenting that timed-out/cancelled blocking-pool jobs occupy one of the fixed 2-8 workers until completion, plus the Phase 5 configurability and saturation/recovery test requirement.
- Captured the ticket 9 pre-change release-binary benchmark on a Mac14,9 Apple M2 Pro with 16 GiB RAM, arm64 macOS 15.7.8 (24G806): over 25 alternating, output-validated runs after five warmups, int32 median was 32.001 ms (MAD 1.135 ms, p95 37.527 ms) and boxed int64 median was 4223.969 ms (MAD 32.941 ms, p95 4274.286 ms), a 131.996x gap.
- Extended the direct scalar ABI to int64 and uint64 across contextual literals, function/task thunks, arithmetic, signed/unsigned comparisons and division, checked overflow, printing, typed boxing/unboxing, and checked unboxed numeric casts. Added exact MIR/direct fixtures for arithmetic, casts, signed upper/lower/division/multiplication/unary-negation overflow, unsigned upper/underflow/multiplication overflow, and cross-signedness cast failures.
- Captured the ticket 9 post-change release-binary benchmark on the same machine and protocol: int32 median was 30.677 ms (MAD 0.071 ms, p95 30.764 ms) and int64 median was 15.806 ms (MAD 0.881 ms, p95 16.755 ms), for an int64/int32 ratio of 0.515x. This passes the required <=1.5x gate.
- Strengthened the ticket 9 acceptance matrix with full-range uint64 task-thunk round trips, the signed `MIN % -1` edge, unsigned unary-negation underflow, wide-to-int32 success and failure, exact and inexact uint64-to-float64 casts, coverage-build FFI calls for every new unboxed helper, and a direct/MIR int64 boxed-boundary overflow diagnostic regression.
- Recorded independent acceptance of ticket 9 and V1-V5 at `856054c`, scheduled the non-blocking V6 int32/int64 direct-loop inversion investigation after Phase 1.5, and resumed the ratified D3 -> D2 -> D4 -> D5 -> D6 sequence after S1-S4 resolved the D2 stop condition.
- Completed the D3 implementation candidate: `int` canonicalizes to `int64`; unhinted positive and negative integer literals, collection elements, generic calls, analysis, MIR, and raw direct operands default to checked unboxed `int64`; expected fixed-width contexts remain authoritative. Integration regressions added contextual `int32` propagation for range/vector-index positions and expected generic results instead of annotating ordinary call sites.
- D3 migration actuals versus the readiness estimate of 25 definite fixture files / 33 sites plus 296 broad candidates and 8 example files / 14 sites: 8 existing fixtures received 12 conservative explicit-`int32` pins, 7 examples received 10 pins, 9 embedded CLI sources received fixed-width context, one broad compiler-coverage source received two pins, and two LSP embedded sources received one Map pin and one Vec-index pin. One overflow-span fixture moved to the `int64` boundary and five existing diagnostic oracles changed inferred type names. The final fixture surface is 14 new logical fixtures (7 check-fail and 7 run-pass) plus 14 changed logical fixtures, spanning 44 files. The maintained reference/tutorial/editor slice touches 21 files: one ADR, nine Manual/proposal files, five tutorials, one reference guard, and five editor source/test files.

## Verification

- V1-V5 checkpoint: 486 compiler library tests and all six fixture suites pass under the repository's 32 MiB serialized test-thread contract; focused failing-first regressions pass; reference, docs build, formatting, and diff checks pass.
- Ticket 9 focused direct-codegen/runtime tests, all six fixture suites, release builds, output validation, the 25-run benchmark gate, the 499-test serialized compiler library suite, the coverage-library FFI test, Clippy with warnings denied, formatting/diff checks, and the full forced-MIR/direct runtime-fixture matrix pass against the completed ticket 9 tree. The expanded parity matrix passed in 217.57 seconds.
- D3 full-gate attempts exposed and migrated eight stale aura CLI sources, then one broad compiler coverage source, then one LSP Map/MapEntry source. Their exact regressions pass; the complete Rust phase passes, all six fixture suites pass, forced MIR/direct parity passes in 221.31 seconds, LSP passes 43/43, VS Code passes 5/5, and every instrumented compiler test passes.
- D3 still fails the enforced compiler coverage floor: 95.91% lines versus 96.01%, 96.84% functions versus 96.71%, and 93.84% regions versus 93.94%. Under the standing stop rule, no threshold was lowered, no coverage-only recovery tests were improvised, no D3 commit was created, and D2 was not started.
- The first G1-authorized behavioral recovery added exact MIR/direct parity for default-int64 boundaries, the canonical `int` ABI, int64 f-string rendering, signed/unsigned comparisons and casts, plus a contextual `range` overflow diagnostic. Every instrumented test passed; coverage improved to 95.93% lines / 96.84% functions / 93.86% regions, still below the frozen line/region floors.
- The second recovery batch adds backend parity for fixed `int32` contexts, a default-int64-to-uint64 underflow path, nested `int` alias field storage, and preservation of generic argument diagnostics. It also consolidates direct rvalue dispatch so valid target-typed Vec/Map/Set/enum paths are no longer duplicated behind structurally unreachable helper arms. The focused fixtures, all-runnable-fixture direct emission, and three real CLI parity tests pass.
- The D3 correctness audit tightened every Vec index surface to exactly `int32`, preserving contextual literals while rejecting bound default-`int64` variables. It also fixed MIR generic receiver/field substitution and static `try` error-width propagation; factored the direct return epilogue so `try` error paths have correct ownership and dominance; and preserved concrete integer width through direct/MIR boxing, casts, arithmetic, class fields, enum payloads, collections, and generic runtime dispatch.
- Seven new D3 run-pass fixtures now pin default `int64`, contextual `int32`, scalar and receiver generic dispatch, nested Box/Option/Vec dispatch, and `try` conversion width. Seven check-fail fixtures pin the `int64` default boundary, no implicit generic narrowing, no implicit signed-to-unsigned conversion, range/Vec exact-index behavior, and retained generic diagnostics.
- The strengthened nested-dispatch parity fixture exposed eager direct-backend class-field inspection on the wrong runtime class. Generic class matching now branches on the outer class before inspecting fields and releases the temporary field handles; MIR and direct both preserve `int32` versus `int64` through `Vec[T] -> Option[T] -> T`.
- The final G1 recovery pins exact observable diagnostics for signed `int64` overflow, unsigned `uint64` underflow, `uint64`-to-`int64` narrowing, and out-of-range float-to-`int32` casts, covering both source-annotated and spanless runtime failures. The same behavioral test family pins the `float32` cast target and negative-float truncation to `int64`.
- The existing nested generic parity fixture now also pins contextual `Option[int64]` inference from a bare `None` argument and specialized dispatch for `Pair[int64, None]`, keeping the last line-gap recovery on real MIR/direct inference and runtime-type results.
- Committed the independently gated D3 migration as `9b49e0d` (`feat: default Aurora integers to int64`) and began D2 test-first.
- The clean D3 compiler coverage gate passes at 96.011359% lines, 96.788009% functions, and 93.983707% regions against the frozen 96.01/96.71/93.94 floors. All 516 instrumented compiler library tests and every instrumented integration suite pass. No synthetic coverage test or coverage-exclusion annotation was added.
- The first final-CI pass completed the full Rust suite and forced-backend parity matrix, then found one stale LSP compiler-bridge buffer whose bound Vec index still defaulted to `int64`. The input now declares that binding as `int32`, and its focused real-compiler LSP regression passes.
- The fresh final D3 `npm run ci` gate passes end to end after that migration: formatting, serialized Rust tests, the 161.22-second forced MIR/direct fixture matrix, 43/43 LSP tests, extension build and 5/5 tests, compiler and LSP coverage checks, reference guards, docs build, npm/cargo audits, warning-denied Clippy, and hygiene are all green.
- Completed the D2 implementation candidate test-first: the lexer/parser accept `//` and `//=`, equal integer `/` and `/=` share the ratified teaching diagnostic, integer floor division and divisor-sign remainder preserve checked widths, floating `//` and `%` share the CPython-compatible divmod correction, and `.to_float()` performs deliberately rounding `float64` conversion on every integer width while `as float64` remains exact.
- Routed D2 through MIR, the boxed native runtime, direct `int32`/`int64`/`uint64` scalar lowering, boxed-width direct calls, compound assignment, analysis/LSP metadata, the editor grammar, and the maintained reference/tutorial/example corpus. The direct float `//`/`%` bridge now consumes operand boxes and releases its borrowed result box after unboxing, closing the independent audit's loop leak.
- Migrated every maintained integer-division corpus use to `//`, including Rust-embedded runtime-error probes, while retaining float `/`. Added `examples/basics/numbers.au`, exact integer `/` and `/=` rejection fixtures, signed and floating floor/remainder fixtures, all-width `.to_float()` rounding coverage, signed-zero and zero-divisor traps, i64 boundary behavior, compound-assignment behavior, and explicit MIR/direct parity tests.
- D2's focused gates pass: all six compiler fixture suites; integer and CPython float-divmod unit proofs; MIR/direct arithmetic, rounding, example, and float-zero parity; 45/45 LSP tests at 100% statements/branches/functions/lines; extension syntax/build tests; reference integrity; docs build; and diff hygiene. No synthetic coverage test or exclusion annotation was added.
- Closed D2's G1 coverage-only gap with observable behavior pins for all twelve integer widths, class/Vec/Map compound places, `.to_float()` in calls/returns/f-strings/casts/collections, scalar `int64` and boxed `int128` overflow boundaries, and both direct-runtime opcode entrypoints. These tests exposed a real parity defect: indexed compound MIR results inherited the defaulted RHS `int64` type instead of the collection element type. Indexed compound lowering now contextually lowers and retargets the RHS and result to the checked Vec/Map element type; MIR and direct execution agree for positive and negative RHS values.
- Restructured five genuinely unreachable defensive/invariant branches instead of forcing them with malformed programs: indexed compound lowering uses its checked local-type invariant directly; the MIR floor-division mismatch and direct `.to_float()` arity diagnostics use shared constants; the validated-away wide-integer fallback is a single diagnostic arm; and native `/` and `//` malformed-operand diagnostics share a helper already exercised by observable `/` rejection behavior. Exact diagnostic text is unchanged and no coverage exclusion annotation was added.
- D2's complete instrumented compiler gate passes with 51,304/53,435 lines (96.011977%), 3,171/3,276 functions (96.794872%), and 74,741/79,519 regions (93.991373%) against the frozen 96.01/96.71/93.94 floors. All 233 CLI tests, 521 compiler library tests, and every instrumented integration suite pass.
- The first fresh full-CI attempt passed formatting, the complete serialized Rust suite, the 176.19-second forced MIR/direct fixture matrix, 45/45 LSP tests, 6/6 extension tests, compiler and 100% LSP coverage, reference guards, docs build, and dependency audits. Warning-denied Clippy then rejected four redundant float-zero guards. The equivalent canonical match patterns are now used, explicit MIR/native positive- and negative-zero division/remainder traps are pinned, the focused runtime tests pass, and Clippy is green before the required full-CI restart.
- The required fresh full-CI restart passes end to end: formatting, the serialized Rust suite, the 170.41-second forced MIR/direct fixture matrix, 45/45 LSP tests, 6/6 extension tests, compiler and 100% LSP coverage, reference guards, docs build, dependency audits, warning-denied Clippy, and hygiene are all green. Final D2 decision coverage is 51,303/53,433 lines (96.013699%), 3,171/3,276 functions (96.794872%), and 74,736/79,511 regions (93.994542%).

## Follow-up

- D3 is complete at `9b49e0d`; D2's implementation and every required gate are complete, with the independent D2 commit next, followed by D4, D5, and D6.
- G1 authorizes behavior-focused D3 coverage recovery and freezes compiler floors at 96.01% lines / 96.71% functions / 93.94% regions through Phase 1.5; synthetic line-execution tests remain forbidden, and one high-water re-ratchet is due at sign-off.
- G2 rejects `own`, `borrow`, and `borrow mut` modifiers on receive-based Queue iteration with a teaching diagnostic; bare Queue iteration remains unchanged.
- G3 permits shared-borrow and `own` parameter defaults, rejects `borrow mut` defaults for both copy and non-copy types with the lost-write diagnostic and two exits, and requires the ADR-0006 amendment plus fixtures.
- D3 coverage recovery identified the direct-codegen `int32`-to-`uint64` implicit-coercion arm as unreachable: checked Aurora rejects implicit widening, explicit casts lower through `Rvalue::Cast`, and target-typed literals emit directly as `uint64`. The arm was removed instead of receiving a synthetic coverage test; no justified coverage exclusion annotation has been added.
- The target-aware direct rvalue helper retains one defensive `Rvalue::Try` invariant arm because assignment lowering must intercept `try` to build its early-return control flow before ordinary target coercion. It is recorded as unreachable rather than receiving a synthetic raw-MIR test; its observable success/error behavior is pinned by the MIR/direct `try` conversion-width fixture.
- The guardrail audit also classified post-`i128::MIN` checked-negation failures, collection fallbacks after an always-present embedded runtime tag, malformed runtime-tag inputs, null FFI pointers, poisoned locks, compiler-invalid integer tags, and direct-codegen invariant residuals as defensive or structurally unreachable. They received neither synthetic tests nor exclusion annotations. D3 instead restructures the unreachable implicit `int32`-to-`uint64` coercion, duplicated target-aware rvalue paths, and the lazy negative-literal-pattern fallback closure while preserving valid behavior.
- The D3 transport audit also reproduced a pre-existing MIR-only limitation for user trait implementations on `Queue[T]`: channel builtin dispatch preempts trait-method fallback and reports `unsupported channel method`. Direct dispatch already selects specialized and blanket implementations correctly. This is not caused by the integer-default migration and is recorded as a post-Phase-1.5 runtime-parity follow-up rather than expanding D3.
- D2's S1-S4 semantics are implemented and awaiting only its gated commit; D4, D5, D6, and Phase 2 have not started.
