import random

trait Describe:
    def describe(self) -> String

impl Describe for random.Rng:
    def describe(self) -> String:
        return "trait rng"

def main() -> int32:
    mut rng = random.Rng(42)
    print(rng.describe())
    print(rng.next_int(0, 10))
    return 0
