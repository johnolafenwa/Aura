class Holder:
    text: String

def show(holder: Holder):
    print(holder.text)

def clone_text(holder: Holder) -> String:
    return holder.text.clone()

def main() -> int32:
    holder = Holder(text="hello")
    show(holder)
    print(clone_text(holder))
    return 0
