def main() -> int32:
    ch: Queue[int32] = queue()
    ch.close()

    send_result = ch.put(7)
    match send_result:
        case Result.Ok(done):
            print("sent")
        case Result.Err(err):
            match err:
                case SendError.Closed(value):
                    print(value)

    return 0
