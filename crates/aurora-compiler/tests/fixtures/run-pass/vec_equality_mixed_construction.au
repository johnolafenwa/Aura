def print_bool(value: bool):
    match own value:
        case true:
            print("true")
        case false:
            print("false")

def main() -> int32:
    literal_numbers: Vec[int32] = [1, 2, 3]
    mut pushed_numbers: Vec[int32] = []
    pushed_numbers.push(1)
    pushed_numbers.push(2)
    pushed_numbers.push(3)

    print_bool(literal_numbers == pushed_numbers)
    print_bool(pushed_numbers == literal_numbers)
    print_bool(literal_numbers != pushed_numbers)

    literal_names = ["Ada", "Grace"]
    mut pushed_names: Vec[String] = []
    pushed_names.push("Ada")
    pushed_names.push("Grace")

    print_bool(literal_names == pushed_names)
    print_bool(pushed_names == literal_names)
    return 0
