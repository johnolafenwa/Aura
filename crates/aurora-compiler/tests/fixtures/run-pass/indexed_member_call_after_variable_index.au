def main() -> int32:
    keys = ["a", "b"]
    idx: int32 = 1
    match keys.get(idx):
        case Option.Some(value):
            print(value.clone())
        case Option.None:
            pass
    return 0
