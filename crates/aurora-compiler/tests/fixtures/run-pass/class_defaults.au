class ServerConfig:
    host: String = "localhost"
    port: int32 = 8080

def main():
    local = ServerConfig()
    named = ServerConfig(host="aurora.dev")

    print(local.host)
    print(named.port)
