trait Mapper[T]:
    def map(borrow self, value: own T) -> T

class Box[T]:
    value: T

impl Mapper[T] for Box[T]:
    def map(borrow self, value: own T) -> T:
        return value

def main() -> int32:
    box = Box(value=7)
    print(box.map(value=11))
    return 0
