def main() -> int32:
    mut labels: Map[String, int64] = {"ready": 1}
    key = "ready"
    labels.remove(key)
    print(key)

    mut tags: Set[String] = {"aurora"}
    probe = "aurora"
    tags.remove(probe)
    print(probe)
    return 0
