def main() -> int32:
    mut values = [1, 2, 3]
    for value in borrow mut values:
        value += 10
        if value == 12:
            continue
        if value == 13:
            break

    for value in borrow values:
        print(value)
    return 0
