def main() -> int32:
    result: Result[String, String] = Result.Ok("ok")

    match borrow result:
        case Ok(value):
            print(value.clone())
        case Err(message):
            print(message)

    match borrow result:
        case Ok(value):
            print(value.clone())
        case Err(message):
            print(message)

    return 0
