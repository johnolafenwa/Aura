class Worker:
    def run(value: int32) -> int32:
        return value + 1

def main() -> int32:
    task = spawn Worker.run(4)
    print(task.result())

    with tasks() as group:
        other = group.start(Worker.run, 6)
        print(other.result())

    return 0
