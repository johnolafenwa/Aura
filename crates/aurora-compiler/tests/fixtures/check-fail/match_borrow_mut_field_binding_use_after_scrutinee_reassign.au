enum Opt:
    Some(int32)
    None

class Holder:
    state: Opt

def main() -> int32:
    mut holder = Holder(state=Opt.Some(10))
    match borrow mut holder.state:
        case Some(v):
            holder.state = Opt.Some(v)
            v = v + 1
        case None:
            pass
    return 0
