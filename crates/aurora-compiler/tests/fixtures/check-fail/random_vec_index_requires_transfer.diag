error[AU3005]: cannot implicitly copy `random.Rng` out of a vector index; `get(index)` cannot clone it because `random.Rng` is directly non-cloneable, so use `remove(index)` to transfer ownership instead
 --> crates/aurora-compiler/tests/fixtures/check-fail/random_vec_index_requires_transfer.au:6:14
  |
6 |     chosen = generators[0]
  |              ^
