class Handle:
    name: Vec[int32]

    def close(borrow mut self):
        print(self.name.len())

def mutate(s: own Vec[int32]) -> int32:
    return s.len()

def main() -> int32:
    with h = Handle(name=[5]):
        value = mutate(h.name)
        print(value)
    return 0
