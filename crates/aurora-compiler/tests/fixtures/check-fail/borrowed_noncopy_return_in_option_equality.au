class User:
    name: String

def name_ref(user: borrow User) -> borrow[user] String:
    return user.name

def main():
    user = User(name="aurora")
    if None == name_ref(user):
        print("unreachable")
