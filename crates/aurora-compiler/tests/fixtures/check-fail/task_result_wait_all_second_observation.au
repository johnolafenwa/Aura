def make_text() -> String:
    return "once"

def main() -> int32:
    with TaskGroup() as group:
        tasks = [group.start(make_text)]
        print(wait_all(tasks))
        print(wait_all(tasks))
    return 0
