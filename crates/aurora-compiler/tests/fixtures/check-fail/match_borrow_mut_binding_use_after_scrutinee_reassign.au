enum Opt:
    Some(int32)
    None

def main() -> int32:
    mut x: Opt = Opt.Some(10)
    match borrow mut x:
        case Some(v):
            x = Opt.Some(v)
            v = v + 1
        case None:
            pass
    return 0
