class Counter:
    value: int32

def increment(counter: mut Counter) -> None:
    counter.value += 1

def main() -> int32:
    shared_only: def(Counter) -> None = increment
    shared_only(Counter(value=0))
    return 0
