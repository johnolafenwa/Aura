enum TrafficLight:
    Red
    Yellow
    Green

def describe(light: own TrafficLight) -> String:
    match own light:
        case TrafficLight.Red:
            return "red"
        case TrafficLight.Yellow:
            return "yellow"
