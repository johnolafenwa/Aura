class Counter:
    value: int32

def show(counter: borrow Counter) -> int32:
    return counter.value

def main() -> int32:
    counter = Counter(value=1)
    task = spawn show(counter)
    print(task.result())
    return 0
