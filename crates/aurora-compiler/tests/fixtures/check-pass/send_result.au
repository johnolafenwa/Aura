def main() -> int32:
    ch: Channel[int32] = channel()
    ch.close()

    send_result = ch.send(7)
    match send_result:
        case Result.Ok(done):
            print("sent")
        case Result.Err(err):
            match err:
                case SendError.Closed(value):
                    print(value)

    return 0
