trait Showable:
    def show(borrow self) -> String

class Box[T]:
    value: T

impl[T] Showable for Box[T]:
    def show(borrow self) -> String:
        return "box"

def main() -> int32:
    value = Box(value=7)
    print(value.show())
    return 0
