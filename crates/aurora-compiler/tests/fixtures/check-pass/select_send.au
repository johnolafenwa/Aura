def main() -> int32:
    ch: Queue[int32] = queue()

    select:
        case send_result = ch.put(4):
            match send_result:
                case Result.Ok(done):
                    print("sent")
                case Result.Err(err):
                    print("failed")
        case after(5ms):
            print("timeout")

    match ch.get():
        case Option.Some(value):
            print(value)
        case Option.None:
            print(0)

    return 0
