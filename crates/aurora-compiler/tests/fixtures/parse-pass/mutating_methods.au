class Counter:
    value: int32

    def bump(mut self):
        self.value += 1

    def reset(mut self):
        self.value = 0

def main() -> int32:
    mut counter = Counter(value=4)
    counter.bump()
    counter.bump()
    counter.reset()
    counter.bump()
    print(counter.value)
    return 0
