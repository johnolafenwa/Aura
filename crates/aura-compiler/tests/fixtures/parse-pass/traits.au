trait Greeter:
    def greet(self) -> str

class User:
    name: str

impl Greeter for User:
    def greet(self) -> str:
        return "hello " + self.name

def speak[T: Greeter](value: T):
    print(value.greet())

def main() -> int32:
    speak(value=User(name="aura"))
    return 0
