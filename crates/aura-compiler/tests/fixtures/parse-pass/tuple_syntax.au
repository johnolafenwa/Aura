def rotate(value: (int32, str), only: (int64,)) -> ((str, int32), bool)?:
    left, label = value
    (single,) = only
    for index, name in rows:
        print((index, name))
    match own value:
        case (0, (name,)):
            return ((name, left), true)
        case _:
            return None
