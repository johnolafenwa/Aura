class User:
    name: String

enum Slot:
    Filled(User)
    Empty

def main():
    mut slot = Slot.Filled(User(name="Ada"))
    match slot:
        case Filled(user):
            alias = user
            print(alias.name)
            slot = Slot.Empty
            print("updated")
        case Empty:
            pass
