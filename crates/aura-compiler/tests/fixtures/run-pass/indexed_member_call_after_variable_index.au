def main() -> int32:
    keys = ["a", "b"]
    idx: int32 = 1
    match keys.get(idx):
        case Lookup.Found(value):
            print(value.clone())
        case Lookup.Missing:
            pass
    return 0
