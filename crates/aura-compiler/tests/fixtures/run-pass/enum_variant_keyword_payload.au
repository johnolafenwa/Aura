enum Status:
    Ready
    Count(int32)

def main() -> int32:
    status = Status.Count(value=4)
    result: Result[int32, str] = Result.Ok(value=7)
    lookup: Lookup[int32] = Found(value=9)
    print(status)
    print(result)
    print(lookup)
    return 0
