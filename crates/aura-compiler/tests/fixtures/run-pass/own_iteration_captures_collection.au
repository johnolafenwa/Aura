def main() -> int32:
    mut values: list[int32] = [1, 2]
    for value in own values:
        print(value)
        values = [7, 8, 9]

    mut members: set[int32] = {1, 2}
    for member in own members:
        print(member)
        members = {7}
    return 0
