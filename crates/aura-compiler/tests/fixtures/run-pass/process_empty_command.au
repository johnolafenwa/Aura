import process

def main() -> int32:
    command: list[str] = []
    match process.run(command):
        case Result.Ok(_):
            print("unexpected")
            return 1
        case Result.Err(error):
            print(error)
            return 0
