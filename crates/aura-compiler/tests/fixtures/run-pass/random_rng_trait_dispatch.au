import random

trait Describe:
    def describe(self) -> str

impl Describe for random.Rng:
    def describe(self) -> str:
        return "trait rng"

def main() -> int32:
    mut rng = random.Rng(42)
    print(rng.describe())
    print(rng.next_int(0, 10))
    return 0
