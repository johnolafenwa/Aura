def print_bool(value: bool):
    match own value:
        case true:
            print("true")
        case false:
            print("false")

def print_int_lookup(value: Lookup[int32]):
    match own value:
        case Found(inner):
            print(inner)
        case Missing:
            print(-1)

def main() -> int32:
    mut counts: dict[str, int32] = {}
    print_bool(counts.is_empty())
    counts["aura"] = 1
    counts["agents"] = 2
    print(counts["aura"])
    print_int_lookup(counts.get("agents"))
    print_bool(("missing" in counts))
    print(counts.len())
    previous_aura = counts.get("aura")
    counts["aura"] = 3
    print_int_lookup(previous_aura)
    print(counts["aura"])
    print_int_lookup(counts.remove("agents"))
    print_bool(counts == {"aura": 3})

    seeded = {"codex": 4, "agents": 5}
    print(seeded["codex"])
    print(seeded.keys().len())
    print(seeded.values().len())
    return 0
