enum Message:
    Text(str)
    Empty

def consume(value: own str):
    print(value)

def main() -> int32:
    message = Message.Text("owned payload")
    match own message:
        case Message.Text(text):
            consume(text)
        case Message.Empty:
            print("empty")
    return 0
