def main() -> int32:
    mut labels: dict[str, int64] = {"ready": 1}
    key = "ready"
    labels.remove(key)
    print(key)

    mut tags: set[str] = {"aura"}
    probe = "aura"
    tags.discard(probe)
    print(probe)
    return 0
