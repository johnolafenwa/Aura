class ServerConfig:
    host: String = "localhost"
    port: int32 = 8080

def main():
    local = ServerConfig()
    named = ServerConfig(host="aura.dev")

    print(local.host)
    print(named.port)
