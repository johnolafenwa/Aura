class Holder:
    text: str

def show(holder: Holder):
    print(holder.text)

def clone_text(holder: Holder) -> str:
    return holder.text.clone()

def main() -> int32:
    holder = Holder(text="hello")
    show(holder)
    print(clone_text(holder))
    return 0
