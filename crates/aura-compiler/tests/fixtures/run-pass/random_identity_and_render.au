import random

def main() -> int32:
    mut first = random.Rng(seed=42)
    mut second = random.Rng(seed=42)
    print(first == first)
    print(first == second)
    print(first)
    print(f"generator={first}")
    print(first.next_float())
    return 0
