class Pair:
    left: str
    right: str

def keep(value: own str) -> str:
    return value

def use(left: str, right: own str):
    print(left)
    print(right)

def main() -> int32:
    pair = Pair(left="left", right="right")
    use(pair.left, keep(pair.right))
    print(pair.left)
    return 0
