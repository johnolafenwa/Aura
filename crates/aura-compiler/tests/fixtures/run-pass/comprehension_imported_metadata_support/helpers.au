public class Boxed:
    value: int64

public def boxed(values: list[int64]) -> list[Boxed]:
    return [Boxed(value=value * 2) for value in values]

public def unboxed(values: list[Boxed]) -> list[int64]:
    return [value.value for value in values]
