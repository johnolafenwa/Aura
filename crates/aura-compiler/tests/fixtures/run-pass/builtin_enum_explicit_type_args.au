def main() -> int32:
    wrapped = Result[int32, str].Ok(7)
    fallback = Result[int32, str].Err("bad")

    match own wrapped:
        case Ok(value):
            print(value)
        case Err(message):
            print(message)

    match own fallback:
        case Ok(value):
            print(value)
        case Err(message):
            print(message)

    return 0
