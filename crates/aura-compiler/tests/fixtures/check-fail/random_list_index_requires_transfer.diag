error[AU3005]: cannot implicitly copy `random.Rng` out of a list index; use `view value = items[index]` for shared access; `.clone()` is unavailable because `random.Rng` is directly non-cloneable, so use `pop(index)` or `set(index, value)` to transfer ownership
 --> crates/aura-compiler/tests/fixtures/check-fail/random_list_index_requires_transfer.au:6:14
  |
6 |     chosen = generators[0]
  |              ^
