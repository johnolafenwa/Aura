def main():
    mut value: int64 | str = 1
    match mut value:
        case int64 as number:
            value = "changed"
            print(number)
        case str as text:
            print(text)
