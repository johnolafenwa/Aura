class Token:
    value: int32

def consume(token: own Token):
    print(token.value)

def main() -> int32:
    token = Token(value=1)
    if true:
        consume(token)
    print(token.value)
    return 0
