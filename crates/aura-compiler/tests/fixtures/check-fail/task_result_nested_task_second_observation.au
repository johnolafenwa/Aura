def make_text() -> str:
    return "nested"

def make_task() -> Task[str]:
    with TaskGroup() as group:
        return group.start(make_text)

def main() -> int32:
    with TaskGroup() as group:
        task = group.start(make_task)
        print(task.result())
        print(task.result())
    return 0
