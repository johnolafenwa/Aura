class Counter:
    total: int64

    def bump(mut self) -> int64:
        self.total = self.total + 1
        return self.total

type Bumper = Callable[mut def() -> int64]

def main():
    counter = Counter(total=0)
    bumper = Bumper(lambda [own counter]: counter.bump())
    print(bumper())
