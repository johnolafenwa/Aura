class Profile:
    name: str
    visits: int64


def main():
    mut users = [Profile(name="ada", visits=1)]
    view first = users[0]
    users.append(Profile(name="linus", visits=2))
    print(first.visits)
