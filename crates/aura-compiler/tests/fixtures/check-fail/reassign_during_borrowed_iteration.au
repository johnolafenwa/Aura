def main() -> None:
    mut values: list[str] = ["aaaa", "bbbb", "cccc"]
    mut step = 0
    for value in values:
        if step == 1:
            values = ["x"]
        print(value)
        step += 1
