def consume_and_measure(values: own list[int32]) -> int32:
    return 2 as int32

def inspect(values: list[int32], measured: int32):
    print(values.len())
    print(measured)

def main():
    values: list[int32] = [1, 2]
    inspect(values, consume_and_measure(values))
