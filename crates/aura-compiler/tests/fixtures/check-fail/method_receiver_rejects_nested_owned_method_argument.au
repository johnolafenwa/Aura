class Consumer:
    def consume(self, values: own list[int32], label: str = "default") -> int32:
        return 0

class Holder:
    values: list[int32]

def main():
    consumer = Consumer()
    holder = Holder(values=[1])
    print(holder.values.get(consumer.consume(holder.values)))
