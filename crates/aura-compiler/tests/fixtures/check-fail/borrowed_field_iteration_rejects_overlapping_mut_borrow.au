class Holder:
    values: list[int32]

def append(value: mut list[int32]):
    value.append(9)

def main():
    mut holder = Holder(values=[1, 2, 3])
    for value in holder.values:
        append(value=holder.values)
