import random

class Node:
    value: str
    next: indirect Node?
    generator: random.Rng

def consume(node: own Node):
    print(node.value)

def launch(group: TaskGroup, node: own Node):
    group.start_soon(consume, node)
