def make_text() -> str:
    return "once"

def main() -> int32:
    with TaskGroup() as group:
        task = group.start(make_text)
        mut count: int32 = 0
        while count < 2:
            print(task.result())
            count += 1
    return 0
