class EntryList:
    entries: list[str]

    def has_entries(self) -> bool:
        match own self.entries:
            case _:
                return true
        return false

def main() -> int32:
    values = EntryList(entries=["a"])
    print(values.has_entries())
    return 0
