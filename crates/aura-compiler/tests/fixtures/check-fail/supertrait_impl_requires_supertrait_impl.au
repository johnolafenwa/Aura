trait Base:
    def base(self) -> int32

trait Derived: Base:
    def derived(self) -> int32

class Counter:
    value: int32

impl Derived for Counter:
    def derived(self) -> int32:
        return self.value

def main() -> int32:
    counter = Counter(value=7)
    return counter.derived()
