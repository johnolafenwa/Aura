class User:
    name: str

enum Slot:
    Filled(User)
    Empty

def main():
    mut slot = Slot.Filled(User(name="Ada"))
    match mut slot:
        case Filled(user):
            alias = user
            print(alias.name)
        case Empty:
            pass
