error[AU3005]: cannot implicitly copy `str` out of a dict index; use `get(key)` for an explicit cloned optional read, or `remove(key)` to transfer ownership
 --> crates/aura-compiler/tests/fixtures/check-fail/map_index_non_copy_requires_explicit_clone.au:3:21
  |
3 |     language: str = labels["language"]
  |                     ^
