def consume(values: own list[int32]) -> Result[int32, str]:
    return Result.Ok(0)

def inspect(values: own list[int32]) -> Result[Option[int32], str]:
    return Result.Ok(values.get(try consume(values)))
