class Pair:
    left: str
    right: str

def left_copy(pair: Pair) -> str:
    return pair.left.clone()

def main():
    pair = Pair(left="ada", right="linus")
    chooser: def(pair: Pair) -> view str from pair = left_copy
    view chosen = chooser(pair)
    print(chosen)
