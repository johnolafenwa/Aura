import control

def main() -> int32:
    payload = "once"
    result = control.retry[str, str](
        lambda: Result[str, str].Ok(payload),
        max_attempts=2,
        initial_backoff=0ms
    )
    return 0
