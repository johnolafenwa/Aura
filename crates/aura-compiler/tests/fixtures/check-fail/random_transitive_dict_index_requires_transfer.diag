error[AU3005]: cannot implicitly copy `Holder` out of a dict index; use `view value = table[key]` for shared access; `.clone()` is unavailable because `Holder` contains non-cloneable `random.Rng` state, so use `remove(key)` to transfer ownership
 --> crates/aura-compiler/tests/fixtures/check-fail/random_transitive_dict_index_requires_transfer.au:9:14
  |
9 |     chosen = holders["a"]
  |              ^
