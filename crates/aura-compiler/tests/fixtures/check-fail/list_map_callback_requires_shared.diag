error[AU2002]: `list.map` callback must take exactly one shared parameter of type `int64`, found `def(own int64) -> int64`
 --> crates/aura-compiler/tests/fixtures/check-fail/list_map_callback_requires_shared.au:6:22
  |
6 |     print(values.map(transfer))
  |                      ^
  = help: declare the callback parameter with the bare type spelling; `mut` and `own` callbacks are not accepted
