import random

def consume(value: own (str, random.Rng)):
    label, _generator = value
    print(label)

def launch(group: TaskGroup, value: own (str, random.Rng)):
    group.start_soon(consume, value)
