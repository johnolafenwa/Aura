trait Scaler:
    def apply(self, value: int64, *, factor: int64) -> int64

class Doubler:
    extra: int64

impl Scaler for Doubler:
    def apply(self, value: int64, factor: int64) -> int64:
        return value * factor + self.extra
