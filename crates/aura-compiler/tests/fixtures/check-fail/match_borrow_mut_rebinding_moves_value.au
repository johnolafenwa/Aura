def main() -> None:
    mut a: list[int32] | None = [1]
    mut b: list[int32] = [7, 8]
    match mut a:
        case list[int32] as v:
            v = b
            v.append(99)
        case None:
            pass
    print(b.len())
