def main() -> None:
    mut a: Option[list[int32]] = Option.Some([1])
    mut b: list[int32] = [7, 8]
    match mut a:
        case Some(v):
            v = b
            v.append(99)
        case None:
            pass
    print(b.len())
