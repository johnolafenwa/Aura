error[AU3007]: cannot use `list.filter` because `random.Rng` contains non-cloneable `random.Rng` state
 --> crates/aura-compiler/tests/fixtures/check-fail/vec_filter_rng_clone_safety.au:8:18
  |
8 |     print(values.filter(keep))
  |                  ^
  = help: move or remove the value so it has one owner, or construct an independent generator with an explicit seed
