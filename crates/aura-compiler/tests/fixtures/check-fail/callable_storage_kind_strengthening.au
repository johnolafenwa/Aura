class Counter:
    total: int64

    def bump(mut self) -> int64:
        self.total = self.total + 1
        return self.total

type Reader = Callable[def() -> int64]

def main():
    counter = Counter(total=0)
    reader = Reader(lambda [own counter]: counter.bump())
    print(reader())
