class Counter:
    value: int32

    def scaled[T](self, factor: T) -> int32:
        return self.value

def main() -> int32:
    counter = Counter(value=1)
    scaled = counter.scaled
    print(scaled(2))
    return 0
