error[AU3005]: cannot implicitly copy `V` out of a dict index; `get(key)` requires a clone-safe `V`, or use `remove(key)` to transfer ownership
 --> crates/aura-compiler/tests/fixtures/check-fail/generic_dict_index_clone_safety_guidance.au:2:12
  |
2 |     return values[key]
  |            ^
