error[AU3005]: cannot implicitly copy `T` out of a list index; `get(index)` requires a clone-safe `T`, or use `pop(index)` to transfer ownership
 --> crates/aura-compiler/tests/fixtures/check-fail/generic_list_index_clone_safety_guidance.au:2:12
  |
2 |     return values[0]
  |            ^
