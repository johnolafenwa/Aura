class Handle:
    name: list[int32]

    def close(mut self):
        print(self.name.len())

def mutate(s: own list[int32]) -> int32:
    return s.len() as int32

def main() -> int32:
    with h = Handle(name=[5]):
        value = mutate(h.name)
        print(value)
    return 0
