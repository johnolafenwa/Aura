class Pair:
    left: str
    right: str

def pick_left(pair: Pair) -> view str from pair:
    return view pair.left

type Job = TaskCallable[def(pair: Pair) -> view str from pair]

def main():
    job = Job(pick_left)
    print(job)
