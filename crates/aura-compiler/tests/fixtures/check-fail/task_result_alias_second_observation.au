def make_text() -> str:
    return "once"

def main() -> int32:
    with TaskGroup() as group:
        task = group.start(make_text)
        alias = task
        print(task.result())
        print(alias.poll())
    return 0
