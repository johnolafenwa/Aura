def main():
    queue = Queue[int64]()
    queue.put(1)
    match queue.get_or_none():
        case Poll.Ready(value):
            print(value)
        case Poll.Unavailable:
            print("empty")
