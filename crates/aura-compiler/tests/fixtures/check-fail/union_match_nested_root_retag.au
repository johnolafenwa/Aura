enum Packet:
    Data(int64 | str)
def main():
    mut value = Packet.Data(41)
    match value:
        case Packet.Data(int64 as number):
            value = Packet.Data("changed")
            print(number)
        case Packet.Data(str as text):
            print(text)
