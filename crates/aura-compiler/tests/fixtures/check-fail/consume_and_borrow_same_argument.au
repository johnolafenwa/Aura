class Holder:
    name: String

def read_ref_take(href: Holder, h: own Holder):
    print(href.name)
    print(h.name)

def main() -> int32:
    h = Holder(name="aura")
    read_ref_take(h, h)
    return 0
