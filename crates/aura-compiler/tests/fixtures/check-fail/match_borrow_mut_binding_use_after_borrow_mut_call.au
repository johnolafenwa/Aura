enum Opt:
    Some(int32)
    None

def replace(value: mut Opt) -> None:
    value = Opt.Some(99)

def main() -> int32:
    mut x: Opt = Opt.Some(10)
    match mut x:
        case Some(v):
            replace(x)
            v = v + 1
        case None:
            pass
    return 0
