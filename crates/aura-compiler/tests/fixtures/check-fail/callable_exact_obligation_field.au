type Destination = Callable[def(value: int64) -> int64]
type Source = TaskCallable[def(value: int64) -> int64]

def target(value: int64) -> int64:
    return value

class Holder:
    callback: Destination

def main():
    holder = Holder(callback=Source(target))
