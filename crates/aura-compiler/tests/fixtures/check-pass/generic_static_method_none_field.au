class Box[T]:
    value: T | None

    def empty() -> Box[T]:
        return Box(value=None)

def main() -> int32:
    box = Box[int32].empty()
    match own box.value:
        case int32 as inner:
            print(inner)
        case None:
            print(0)
    return 0
