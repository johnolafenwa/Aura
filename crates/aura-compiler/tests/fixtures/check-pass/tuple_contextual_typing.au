def make_record() -> ((Option[int32], int32), (str,)):
    return ((None, 7), ("ready",))

def inspect(record: own ((Option[int32], int32), (str,))):
    (maybe, count), (label,) = record
    match (label, count):
        case (name, value):
            pass

def inspect_rows(rows: list[(str, int32)]):
    for label, value in rows:
        pass

def main():
    record: ((Option[int32], int32), (str,)) = ((None, 7), ("ready",))
    inspect(record)
    inspect(make_record())
