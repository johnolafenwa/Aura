enum Wrapped[T]:
    Value(T)

def main():
    mut optionals: list[int32 | None] = []
    optionals.append(1)
    optionals.append(None)

    mut results: list[Result[int32, str]] = []
    results.append(Result.Ok(2))

    mut wrapped: list[Wrapped[int32]] = []
    wrapped.append(Wrapped[int32].Value(3))
