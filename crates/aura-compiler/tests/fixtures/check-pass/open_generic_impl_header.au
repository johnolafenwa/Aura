trait Showable:
    def show(self) -> str

class Box[T]:
    value: T

impl[T] Showable for Box[T]:
    def show(self) -> str:
        return "box"

def main() -> int32:
    value = Box(value=7)
    print(value.show())
    return 0
