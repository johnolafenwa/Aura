trait Named:
    def name(self) -> str

class User:
    label: str

impl Named for User:
    def name(self) -> str:
        return self.label.clone()

class Wrapper[T: Named]:
    value: T

enum MaybeNamed[T: Named]:
    Some(T)
    Empty

def main() -> int32:
    wrapped = Wrapper(value=User(label="aura"))
    value: MaybeNamed[User] = MaybeNamed[User].Empty
    print(wrapped.value.name())
    match own value:
        case MaybeNamed.Some(inner):
            print(inner.name())
        case _:
            print("empty")
    return 0
