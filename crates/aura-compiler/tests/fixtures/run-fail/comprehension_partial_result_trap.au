class Resource:
    name: str

    def close(mut self):
        print("close " + self.name)

def build_value(index: int64) -> str:
    print(f"build {index}")
    if index == 2:
        empty: list[int64] = []
        impossible = empty[0]
        return f"value-{impossible}"
    return f"value-{index}"

def main():
    with resource = Resource(name="outer"):
        result: list[str] = [
            build_value(index)
            for index in [1, 2, 3]
        ]
        print(result)
