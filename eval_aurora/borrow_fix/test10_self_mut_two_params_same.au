# Test 10: borrow mut self + two params, all aliased — should REJECT
class Acc:
    val: int32

    def merge(borrow mut self, a: borrow mut Acc, b: borrow mut Acc):
        self.val += a.val + b.val

def main():
    mut x = Acc(val=5)
    x.merge(a=x, b=x)
    print(x.val)
