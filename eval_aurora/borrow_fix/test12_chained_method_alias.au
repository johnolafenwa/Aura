# Test 12: Method call where param is a field's parent — should REJECT if same object
class Container:
    count: int32

    def add_count(borrow mut self, other: borrow Container):
        self.count += other.count

def main():
    mut c = Container(count=7)
    c.add_count(other=c)
    print(c.count)
