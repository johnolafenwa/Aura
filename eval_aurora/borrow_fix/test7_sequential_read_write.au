class Wrapper:
    value: int32

def read_it(w: borrow Wrapper) -> int32:
    return w.value

def write_it(w: borrow mut Wrapper):
    w.value += 1

def main():
    mut w = Wrapper(value=0)
    v = read_it(w=w)
    print(v)
    write_it(w=w)
    print(w.value)
