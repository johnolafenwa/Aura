def main() -> int32:
    jobs: Channel[int32] = channel()
    jobs.send(1)
    jobs.send(2)
    jobs.send(4)
    jobs.close()
    mut total: int32 = 0
    for job in jobs:
        total += job
    print(total)
    return 0
