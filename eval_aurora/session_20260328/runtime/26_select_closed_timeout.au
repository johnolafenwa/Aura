def main() -> int32:
    ch: Channel[int32] = channel()
    ch.close()
    select:
        case value = ch.recv():
            match value:
                case Option.Some(v):
                    print(v)
                case Option.None:
                    print(1)
        case after(1ms):
            print(2)
    return 0
