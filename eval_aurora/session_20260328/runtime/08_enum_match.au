enum Message:
    Ping
    Text(String)

def describe(msg: Message) -> String:
    match msg:
        case Message.Ping:
            return "ping"
        case Message.Text(text):
            return text.clone()

def main() -> int32:
    print(describe(Message.Ping))
    print(describe(Message.Text("hello")))
    return 0
