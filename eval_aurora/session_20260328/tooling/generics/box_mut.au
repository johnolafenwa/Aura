class Box[T]:
    value: T

    def replace(borrow mut self, value: T) -> T:
        old = self.value
        self.value = value
        return old

def main() -> int32:
    mut box = Box(value=7)
    print(box.replace(value=11))
    print(box.value)
    return 0
