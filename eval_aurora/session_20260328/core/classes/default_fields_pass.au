class ServerConfig:
    host: String = "localhost"
    port: int32 = 8080


def main() -> int32:
    local = ServerConfig()
    named = ServerConfig(host="aurora.dev")

    print(local.host)
    print(named.port)
    return 0
