def main() -> int32:
    mut m = Map[String, int32]()
    m.set("a", 1)
    m.set("b", 2)
    m.set("c", 3)
    keys = m.keys()
    print(keys.len())
    for k in borrow keys:
        print(k)
    return 0
