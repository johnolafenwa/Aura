def main() -> int32:
    mut m = Map[String, Vec[int32]]()
    m.set("evens", [2, 4, 6])
    m.set("odds", [1, 3, 5])
    evens = m["evens"]
    for v in borrow evens:
        print(v)
    odds = m["odds"]
    for v in borrow odds:
        print(v)
    return 0
