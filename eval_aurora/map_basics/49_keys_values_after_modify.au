def main() -> int32:
    mut m = {"a": 1, "b": 2, "c": 3}
    m.remove("b")
    m.set("d", 4)
    keys = m.keys()
    for k in borrow keys:
        print(k)
    print("---")
    values = m.values()
    for v in borrow values:
        print(v)
    return 0
