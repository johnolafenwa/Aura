def main() -> int32:
    mut v = [1, 2, 3, 4, 5]
    for x in borrow mut v:
        if x == 3:
            continue
        x = x * 10
    # All except 3 should be multiplied by 10
    for x in borrow v:
        print(x)
    return 0
