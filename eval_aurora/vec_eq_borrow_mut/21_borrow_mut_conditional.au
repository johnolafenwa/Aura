def main() -> int32:
    mut v = [1, 2, 3, 4, 5]
    # Only double even numbers
    for x in borrow mut v:
        if x % 2 == 0:
            x = x * 2
    for x in borrow v:
        print(x)
    return 0
