def main() -> int32:
    mut v = [1, 2, 3, 4, 5]
    for x in borrow mut v:
        x = x * 10
        if x == 30:
            break
    # First 3 should be modified, last 2 should be original
    for x in borrow v:
        print(x)
    return 0
