def main() -> int32:
    result = "Hi".strip_prefix("Hello")
    match result:
        case Some(inner):
            print("FAIL: expected None, got: " + inner)
        case None:
            print("None")
    return 0
