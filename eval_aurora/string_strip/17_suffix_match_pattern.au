def strip_extension(filename: String) -> String:
    match filename.strip_suffix(".au"):
        case Some(base):
            return base
        case None:
            return filename

def main() -> int32:
    print(strip_extension("main.au"))
    print(strip_extension("readme.txt"))
    return 0
