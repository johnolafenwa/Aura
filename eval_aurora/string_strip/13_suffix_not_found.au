def main() -> int32:
    result = "HelloWorld".strip_suffix("xyz")
    match result:
        case Some(inner):
            print("FAIL: expected None, got " + inner)
        case None:
            print("None")
    return 0
