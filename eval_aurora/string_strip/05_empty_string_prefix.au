def main() -> int32:
    result = "".strip_prefix("a")
    match result:
        case Some(inner):
            print("FAIL: expected None, got: " + inner)
        case None:
            print("None")
    return 0
