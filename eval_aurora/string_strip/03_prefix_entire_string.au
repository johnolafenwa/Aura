def main() -> int32:
    result = "Hello".strip_prefix("Hello")
    match result:
        case Some(inner):
            print("got empty: " + inner + ".")
            print(inner.len())
        case None:
            print("FAIL: expected Some")
    return 0
