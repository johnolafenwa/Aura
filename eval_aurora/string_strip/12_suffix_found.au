def main() -> int32:
    result = "HelloWorld".strip_suffix("World")
    match result:
        case Some(inner):
            print(inner)
        case None:
            print("FAIL: expected Some")
    return 0
