def main() -> int32:
    result = "HelloWorld".strip_prefix("Hello")
    match result:
        case Some(inner):
            print(inner)
        case None:
            print("FAIL: expected Some")
    return 0
