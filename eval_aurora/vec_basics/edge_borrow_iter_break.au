def main() -> int32:
    mut v = [1, 2, 3, 4, 5]

    # Can we break from borrow iteration?
    for x in borrow v:
        if x == 3:
            break
        print(x)

    # v should still be usable after break
    print(v.len())

    return 0
