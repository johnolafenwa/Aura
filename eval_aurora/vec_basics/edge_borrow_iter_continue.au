def main() -> int32:
    mut v = [1, 2, 3, 4, 5]

    # Can we continue in borrow iteration?
    for x in borrow v:
        if x == 3:
            continue
        print(x)

    # v should still be usable
    print(v.len())
    v.push(6)
    print(v.len())

    return 0
