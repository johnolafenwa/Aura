# Test 3: Use after move - compiler should reject this
class Data:
    name: String

def consume(d: Data):
    print(d.name)

def main() -> int32:
    data = Data(name="hello")
    consume(data)
    # This should be a compile error - data was moved into consume
    print(data.name)
    return 0
