# Test 4b: Confirm clone() does NOT consume the original
def main() -> int32:
    text: String = "original"
    cloned = text.clone()
    # Use cloned
    print(cloned)
    # Use original after clone - should this work?
    print(text)
    # Use original again
    print(text.clone())
    return 0
