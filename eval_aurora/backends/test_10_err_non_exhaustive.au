enum Color:
    Red
    Green
    Blue

def name(c: Color) -> String:
    match c:
        case Color.Red:
            return "red"
        case Color.Green:
            return "green"

def main() -> int32:
    print(name(Color.Red))
    return 0
