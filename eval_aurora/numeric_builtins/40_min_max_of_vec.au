def main() -> int32:
    v = [5, 3, 8, 1, 9, 2, 7]
    mut lo: int32 = v[0]
    mut hi: int32 = v[0]
    for item in borrow v:
        lo = min(lo, item)
        hi = max(hi, item)
    print(lo)
    print(hi)
    return 0
