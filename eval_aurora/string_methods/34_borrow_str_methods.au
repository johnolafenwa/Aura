def check_length(s: borrow str) -> int32:
    return s.len()

def check_contains(s: borrow str) -> bool:
    return s.contains("world")

def main() -> int32:
    msg = "hello world"
    print(check_length(msg))
    print(check_contains(msg))
    return 0
