# Test 12: Calling convention — named args index= and value=
def main() -> int32:
    mut v = [10, 30]
    # Named args
    v.insert(index=1, value=20)
    print(v[0])
    print(v[1])
    print(v[2])
    return 0
