# Test 18: Function that builds and returns a vec

def make_range(n: int32) -> Vec[int32]:
    mut result: Vec[int32] = []
    for i in range(n):
        result.push(i)
    return result

def main() -> int32:
    xs = make_range(5)
    for x in xs:
        print(x)
    return 0
