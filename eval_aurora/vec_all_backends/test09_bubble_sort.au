def main() -> int32:
    mut v = [5, 3, 1, 4, 2]
    n = v.len()
    for i in range(n):
        for j in range(n - 1 - i):
            if v[j] > v[j + 1]:
                v.swap(j, j + 1)
    for x in borrow v:
        print(x)
    return 0
