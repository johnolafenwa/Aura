# Test 22: Vec passed by borrow - caller keeps it
def read_vec(v: borrow Vec[int32]):
    print(v.len())
    for x in borrow v:
        print(x)

def main() -> int32:
    v = [10, 20, 30]
    read_vec(v)
    # v should still be usable after borrow
    print(v.len())
    print(v[0])
    return 0
