# Test 10: Very large index on small vec
def main() -> int32:
    v = [1, 2, 3]
    result = v.get(999999)
    match result:
        case Some(val):
            print(val)
        case None:
            print("large index returned None")
    return 0
