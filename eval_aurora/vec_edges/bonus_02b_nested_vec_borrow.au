# Bonus 2b: Nested vec access via single index (does first access move?)
def main() -> int32:
    mut outer: Vec[Vec[int32]] = []
    outer.push([1, 2, 3])
    outer.push([4, 5, 6])
    # Try single nested access only
    print(outer[0][1])
    return 0
