# Bonus: Nested vec access v[i][j]
def main() -> int32:
    mut outer: Vec[Vec[int32]] = []
    outer.push([1, 2, 3])
    outer.push([4, 5, 6])
    # Can we do chained indexing?
    print(outer[0][1])
    print(outer[1][2])
    return 0
