# Test 21b: Vec passed to function by value - just the move, no use after
def consume(v: Vec[int32]):
    print(v.len())
    for x in v:
        print(x)

def main() -> int32:
    v = [1, 2, 3]
    consume(v)
    return 0
