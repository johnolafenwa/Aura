def greet(name: str) -> str:
    return "hello " + name

def shout(msg: str) -> str:
    return msg + "!"

def chain(a: str, b: str) -> str:
    return a + " and " + b

def main() -> int32:
    result = greet("aura")
    print(result)

    loud = shout("wow")
    print(loud)

    both = chain("alpha", "beta")
    print(both)

    return 0
