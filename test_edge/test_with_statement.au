## Test 18: With statement - resource cleanup with close()
class Resource:
    name: str

    def use(self) -> str:
        return "using " + self.name

    def close(mut self):
        print("closing " + self.name)

def work() -> Result[str, str]:
    with r = Resource(name="database"):
        msg = r.use()
        print(msg)
        return Result.Ok("done")

def main() -> int32:
    match work():
        case Result.Ok(v):
            print(v)
        case Result.Err(e):
            print(e)
    return 0
