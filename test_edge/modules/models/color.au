public enum Color:
    Red
    Green
    Blue

public def color_name(c: Color) -> String:
    match own c:
        case Color.Red:
            return "red"
        case Color.Green:
            return "green"
        case Color.Blue:
            return "blue"
