class Person:
    name: str
    title: str

    def full_title(self) -> str:
        return self.title + " " + self.name

class Config:
    host: str = "localhost"
    path: str = "/api"

    def url(self) -> str:
        return self.host + self.path

def main() -> int32:
    p = Person(name="Ada", title="Dr")
    print(p.full_title())
    print(p.name)
    print(p.title)

    c = Config()
    print(c.url())

    c2 = Config(host="example.com")
    print(c2.url())

    return 0
