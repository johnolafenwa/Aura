# BUG TEST: Can Result.Ok be constructed without return type context?

def main() -> int32:
    # With explicit type annotation
    r1: Result[int32, str] = Result.Ok(42)
    match own r1:
        case Result.Ok(v):
            print(v)
        case Result.Err(e):
            print(e)

    # Without type annotation (should be inferred)
    r2 = Result.Ok(99)
    match own r2:
        case Result.Ok(v):
            print(v)
        case Result.Err(e):
            print(e)

    return 0
