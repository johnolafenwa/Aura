class Token:
    value: String

    def consume(own self) -> String:
        return self.value

    def consume_and_print(own self):
        print(self.value)

class Wrapper:
    inner: int32

    def unwrap(own self) -> int32:
        return self.inner

def main() -> int32:
    t = Token(value="secret")
    s = t.consume()
    print(s)

    t2 = Token(value="hello")
    t2.consume_and_print()

    w = Wrapper(inner=42)
    print(w.unwrap())

    return 0
