## Test: Deeply nested function calls
def add(a: int32, b: int32) -> int32:
    return a + b

def double(n: int32) -> int32:
    return n * 2

def main() -> int32:
    result = double(n=add(a=double(n=3), b=add(a=1, b=2)))
    print(result)
    return 0
