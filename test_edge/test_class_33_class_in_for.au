# Edge: using classes inside a for loop
class Counter:
    value: int32

    def bump(mut self):
        self.value += 1

def main() -> int32:
    mut c = Counter(value=0)
    for i in range(10):
        c.bump()
    print(c.value)
    return 0
