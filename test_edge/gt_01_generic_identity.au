## Test 01: Generic identity function with single type param
## Expects: 42 then "hello"

def identity[T](value: own T) -> T:
    return value

def main() -> int32:
    x = identity(42)
    print(x)
    s = identity("hello")
    print(s)
    return 0
