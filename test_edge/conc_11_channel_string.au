def string_sender(ch: Channel[str]):
    ch.send("hello")
    ch.send("aura")
    ch.send("world")
    ch.close()

def main() -> int32:
    ch: Channel[str] = channel()
    task = spawn string_sender(ch.clone())

    mut count: int32 = 0
    while true:
        match ch.recv():
            case Option.Some(v):
                print(v)
                count += 1
            case Option.None:
                break

    task.join()
    print(count)
    print("string channel ok")
    return 0
