## Test 48: Generic enum with two type parameters
## Expects: "left: 42" then "right: hello"

enum Either[L, R]:
    Left(L)
    Right(R)

def main() -> int32:
    e1: Either[int32, str] = Either.Left(42)
    match own e1:
        case Either.Left(v):
            print(v)
        case Either.Right(v):
            print(v)

    e2: Either[int32, str] = Either.Right("hello")
    match own e2:
        case Either.Left(v):
            print(v)
        case Either.Right(v):
            print(v)
    return 0
