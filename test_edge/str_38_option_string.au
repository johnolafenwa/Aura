# Test: Option[str] with various patterns

def find(query: str) -> Option[str]:
    if query == "yes":
        return Option.Some(f"found: {query}")
    return Option.None

def main() -> int32:
    # Some case
    r1 = find("yes")
    match own r1:
        case Option.Some(v):
            print(v)
        case Option.None:
            print("not found")

    # None case
    r2 = find("no")
    match own r2:
        case Option.Some(v):
            print(v)
        case Option.None:
            print("not found")

    # Nested: Option in Option
    inner: Option[str] = Option.Some("inner_value")
    match own inner:
        case Option.Some(s):
            print(s)
        case Option.None:
            print("none")

    return 0
