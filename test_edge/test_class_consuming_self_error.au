class Token:
    value: str

    def consume(own self) -> str:
        return self.value

def main() -> int32:
    t = Token(value="secret")
    s = t.consume()
    print(s)
    print(t.value)
    return 0
