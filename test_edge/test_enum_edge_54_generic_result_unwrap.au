# Test: generic function to unwrap Result or return default
def unwrap_result_or[T](res: own Result[T, str], default_val: own T) -> T:
    match own res:
        case Result.Ok(v):
            return v
        case Result.Err(e):
            print(e)
            return default_val

def main() -> int32:
    r1: Result[int32, str] = Result.Ok(42)
    r2: Result[int32, str] = Result.Err("oops")
    print(unwrap_result_or(r1, 0))
    print(unwrap_result_or(r2, -1))

    r3: Result[str, str] = Result.Ok("success")
    r4: Result[str, str] = Result.Err("fail")
    print(unwrap_result_or(r3, "default"))
    print(unwrap_result_or(r4, "default"))

    return 0
