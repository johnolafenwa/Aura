# This should produce a compile error: use after move

def consume(text: str):
    print(text)

def main() -> int32:
    s: str = "hello"
    consume(text=s)
    # s is moved, this should error
    print(s)
    return 0
