enum Singleton:
    Only(int32)

def get(s: Singleton) -> int32:
    match own s:
        case Singleton.Only(v):
            return v

def main() -> int32:
    s = Singleton.Only(7)
    print(get(s))
    return 0
