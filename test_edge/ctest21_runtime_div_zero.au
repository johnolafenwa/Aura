def get_zero() -> int32:
    mut x: int32 = 1
    x -= 1
    return x

def main() -> int32:
    # Division by zero with computed zero - can the compiler detect this?
    a: int32 = 10
    b: int32 = get_zero()
    print(a // b)
    return 0
