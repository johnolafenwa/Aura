# Test borrow mut exclusivity: passing immutable to borrow mut should error

class Data:
    value: int32

def modify(d: borrow mut Data):
    d.value += 1

def main() -> int32:
    # Non-mutable variable passed to borrow mut should error
    data = Data(value=10)
    modify(data)
    return 0
