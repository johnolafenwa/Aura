enum Maybe[T]:
    Just(T)
    Nothing

def make_nothing() -> Maybe[int32]:
    return Maybe.Nothing

def main() -> int32:
    m = make_nothing()
    match m:
        case Maybe.Just(v):
            print(v)
        case Maybe.Nothing:
            print("nothing")
    return 0
