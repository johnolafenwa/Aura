# Investigate: free function with borrow param, accessing str fields

class Holder:
    text: str

def get_text(h: Holder) -> str:
    return h.text

def main() -> int32:
    h = Holder(text="hello")
    t = get_text(h)
    print(t)
    return 0
