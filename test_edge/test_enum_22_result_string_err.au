def parse_positive(s: str) -> Result[int32, str]:
    return Result.Err("not implemented yet")

def main() -> int32:
    r = parse_positive("hello")
    match own r:
        case Result.Ok(v):
            print(v)
        case Result.Err(e):
            print(e)
    return 0
