## Test: Deep recursion to test stack behavior
def count_down(n: int32) -> int32:
    if n <= 0:
        return 0
    return count_down(n=n - 1)

def main() -> int32:
    result = count_down(n=10000)
    print(result)
    return 0
