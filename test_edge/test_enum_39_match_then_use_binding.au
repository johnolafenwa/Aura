enum Data:
    Num(int32)
    Txt(str)

def main() -> int32:
    d = Data.Num(7)
    mut result = 0
    match own d:
        case Data.Num(v):
            result = v * 3
        case Data.Txt(s):
            print(s)
    print(result)
    return 0
