class Inner:
    data: str

class Outer:
    inner: Inner
    label: str

def main() -> int32:
    mut a = Outer(inner=Inner(data="original"), label="A")
    mut b = Outer(inner=Inner(data="other"), label="B")

    b.inner = Inner(data="replaced")
    print(b.inner.data)

    return 0
