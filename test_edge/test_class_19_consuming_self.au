# Edge: consuming self method (should take ownership)
class Token:
    value: String

    def consume(own self) -> String:
        return self.value

def main() -> int32:
    t = Token(value="secret")
    result = t.consume()
    print(result)
    return 0
