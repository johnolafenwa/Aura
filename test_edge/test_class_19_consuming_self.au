# Edge: consuming self method (should take ownership)
class Token:
    value: str

    def consume(own self) -> str:
        return self.value

def main() -> int32:
    t = Token(value="secret")
    result = t.consume()
    print(result)
    return 0
